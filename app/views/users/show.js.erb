$("#user_posts").append("<%= j(render(@user_posts)) %>");
<% if @user_posts.next_page %>
  $('.pagination').replaceWith("<%= j(will_paginate(@user_posts)) %>");
<% else %>
  $('.pagination').remove();
<% end %>